# MoneyFlow 2.1.5

Status: Backend Financial Insight foundation in progress. P12A through P12F are complete.

Release target: backend Financial Insight foundation.

## Boundary

2.1.5 starts the Financial Insight backend module. Current work is read-only backend foundation code and docs. It does not add frontend UI, scheduled jobs, migrations, or AI calls.

## Product Direction

Financial Insight should answer practical questions from real ledger data:

- Where did income come from?
- Where did spending go?
- Which categories and jars are driving risk?
- Which income has no wallet and therefore does not change wallet balance?
- Which records need user cleanup before analysis is complete?

Bad insight: generic advice without evidence.

Good insight: a deterministic statement backed by category amount, baseline, date range, exclusions, and action route.

## Phase Status

| Phase | Status | Scope |
| --- | --- | --- |
| P12A | Complete | Backend metric audit, insight contract, release plan. |
| P12B | Complete | Internal metric query layer. |
| P12C | Complete | Spending/category/jar insight rules. |
| P12D | Complete | Actually spendable insight backend. |
| P12E | Complete | Action items and data quality insights. |
| P12F | Complete | Insight API endpoints. |
| P12G | Planned | Release lock and validation. |

## P12B Runtime Additions

- Added `FinancialMetricQueryService`.
- Added internal metric DTO records.
- Added aggregate metrics for income, expense, net cashflow, income source, no-wallet income, category spending, jar spending, uncategorized spending, and wallet-affecting summary.
- Added tests for formula exclusions, date boundaries, workspace isolation, and empty state.

## P12C Runtime Additions

- Added internal insight card and evidence model.
- Added deterministic spending rule engine.
- Implemented category overspend, jar overspend, spending spike, uncategorized spending, concentration, and no-wallet income cards.
- Added trailing 3-period baseline behavior.
- Added ranking by severity, amount, and deterministic key.
- Added targeted unit tests for insight rules only.

## P12D Runtime Additions

- Added internal actually-spendable snapshot service.
- Added snapshot, source breakdown, and data quality warning DTOs.
- Formula: available ledger minus active reserves, upcoming required outflows, and overdue required outflows.
- Expected incoming is returned separately and not counted as spendable.
- No-wallet income is reported as excluded from spendable.
- Added targeted unit tests for spendable calculation only.

## P12E Runtime Additions

- Added internal financial action item model and report.
- Added grouped action item detection service.
- Added data quality mappings from actually-spendable warnings.
- Added action items for missing category/wallet, no-wallet income, voice drafts, receipt/OCR drafts, debt missing due date, historical analytics-only data, low/negative spendable, and partial planning data.
- Added targeted unit tests for action item detection only.

## P12F Runtime Additions

Base path: `/api/workspaces/{workspaceId}/insights`

- `GET /overview`
- `GET /metrics?from=YYYY-MM-DD&to=YYYY-MM-DD`
- `GET /cards?from=YYYY-MM-DD&to=YYYY-MM-DD`
- `GET /actually-spendable`
- `GET /action-items`

P12F added stable response DTOs for totals, metric breakdowns, insight cards, actually spendable snapshots, action items, no-wallet income, and data quality warnings. Every endpoint verifies workspace membership before calling insight services. All endpoints are GET-only and reuse deterministic backend services.

Default behavior:

- `overview` and `action-items` default to the current month.
- `period=today|week|month|custom` is supported for overview.
- `horizonDays` defaults to `30`.
- `maxCards` defaults to `8` and is capped at `20`.

Validation:

- invalid date format: `INVALID_INSIGHT_DATE`
- invalid range or incomplete custom range: `INVALID_INSIGHT_DATE_RANGE`
- invalid period: `INVALID_INSIGHT_PERIOD`
- invalid numeric params: `INVALID_INSIGHT_PARAMETER`

Targeted tests:

`.\mvnw.cmd "-Dtest=*FinancialInsightController*Tests,*FinancialInsightApi*Tests,*InsightApi*Tests" test`

## Non-Negotiables

- Insights are read-only.
- Insights must be grounded in real backend data.
- No fake runtime insight data.
- No external AI calls in 2.1.5 backend foundation.
- No unconfirmed voice/receipt drafts in ledger metrics.
- No transfer or debt movement counted as normal income/expense.
- No-wallet income counts for income stats but not wallet balance.
- Actually spendable must reuse planning rules and show exclusions.
- Every later insight card must include evidence or a data quality warning.

## Known Gaps

- No user-facing insight dashboard exists yet.
- No AI text generation exists.
- No ML anomaly detection exists.
- UI is not implemented yet.
