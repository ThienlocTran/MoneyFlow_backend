# MoneyFlow 2.1.6

Status: COMPLETE / BACKEND LOCKED. P13A through P13G are complete for backend/API scope.

Release target: Planning Backend.

## Boundary

2.1.6 is a backend-first planning release. It locks upcoming obligations, reserves, projection, and actually-spendable planning APIs. Frontend Planning UI remains deferred.

## Planned Scope

- Planning backend audit and contract.
- Planned obligation model/API foundation.
- Reserve allocation model/API foundation or safe reuse of existing reserve modules.
- Planning projection service.
- Mark-paid/link-to-transaction behavior.
- Planning overview API and action item integration.
- Backend release lock.

## Deferred Scope

- Frontend planning dashboard redesign.
- AI/LLM planning advice.
- Notifications and scheduled jobs.
- Automatic transaction posting from due dates.
- Fake expected income.
- Broad jar/category/debt redesign unless a later phase proves it necessary.

## Current Phase Status

| Phase | Status | Scope |
| --- | --- | --- |
| P13A | Complete | Planning backend audit, contract, release plan, release stub. |
| P13B | Complete | Planned obligation model and CRUD foundation. |
| P13C | Complete | Reserve allocation model and CRUD foundation. |
| P13D | Complete | Planning projection service. |
| P13E | Complete | Mark-paid/link-to-transaction behavior. |
| P13F | Complete | Planning API overview and action item integration. |
| P13G | Complete | Planning backend release lock. |

## Release Lock Table

Release: 2.1.6

Theme: Planning Backend

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Planning audit/contract | Complete | P13A docs exist under `docs/planning`. | Historical audit kept as baseline. |
| Planned obligation model | Complete | `planned_obligations` migration/entity/status/priority/recurrence fields. | One-off foundation; recurrence generation deferred. |
| Planned obligation CRUD | Complete | Create/list/detail/update/cancel API and integration tests. | CRUD has no wallet balance side effects. |
| Reserve allocation model | Complete | `reserve_allocations` migration/entity/status/purpose fields. | Thin planning reserve model only. |
| Reserve allocation CRUD | Complete | Create/list/detail/update/release/cancel API and integration tests. | Reserve does not move money. |
| Projection formula | Complete | `PlanningProjectionService`. | `availableLedger - activeReserve - upcomingRequiredOutflow - overdueRequiredOutflow`. |
| Actually spendable / projected shortfall | Complete | Projection DTOs and warnings. | Negative spendable is not clamped; shortfall is separate. |
| Mark-paid / link transaction | Complete | `link-transaction` and `mark-paid` endpoints. | Explicit user action only. |
| No auto-pay invariant | Complete | P13E tests and docs. | Due date alone never creates a transaction. |
| Planning overview API | Complete | `GET /planning/overview`. | Read-only. |
| Obligation summary API | Complete | `GET /planning/obligations/summary`. | Read-only. |
| Reserve summary API | Complete | `GET /planning/reserves/summary`. | Read-only. |
| Planning action items | Complete | Overview-local action items. | Financial Insight action service integration deferred. |
| Workspace/security isolation | Complete | Service membership checks and workspace-scoped repositories/tests. | Cross-workspace data must not leak. |
| No ledger side effects | Complete | CRUD/release/cancel/read tests and docs. | Only `mark-paid` creates an explicit posted expense through `TransactionService`. |
| Targeted tests | Passed | P13G targeted Planning Maven command. | 57 tests passed, 0 failures, 0 errors, 0 skipped. |
| Frontend/UI status | Deferred | Backend release scope only. | Frontend Planning UI not implemented in 2.1.6 backend lock. |
| Known limitations | Documented | Known limitations section. | No backend blocker for release lock. |

## Current Backend Evidence

- Existing planning API: `/api/workspaces/{workspaceId}/planning/actually-spendable`.
- Existing planning preferences API: `/planning/preferences`.
- New planned obligation API: `/api/workspaces/{workspaceId}/planning/obligations`.
- New reserve allocation API: `/api/workspaces/{workspaceId}/planning/reserves`.
- Existing recurring obligations and occurrence confirmation APIs.
- Existing sinking fund, savings goal, and emergency fund reserve ledgers.
- Existing debt module with payable/receivable, due date, remaining amount, and payment history.
- Existing wallet balance service grounded in posted wallet-affecting transactions.
- New planning projection service over wallet ledger, planning reserves, and planned obligations.
- New planned obligation payment endpoints for link existing transaction and explicit mark-paid.
- New planning overview/projection/summary APIs for frontend consumption.
- Existing P12 Financial Insight spendable/action item patterns.

## P13B Runtime Additions

- Added `planned_obligations`.
- Added planned obligation status, priority, recurrence type, and computed state.
- Added create/list/detail/update/cancel endpoints.
- Added validation for required name, positive amount, required due date, currency, priority, recurrence, and same-workspace wallet/category references.
- Added cancel behavior: `PLANNED -> CANCELLED`, repeated cancel returns cancelled state, `PAID` cancel blocked.
- Added recurrence storage only; generation is deferred.
- Added targeted integration tests proving workspace isolation and no transaction side effects.

Targeted tests:

`.\mvnw.cmd "-Dtest=*PlannedObligation*Tests,*PlanningObligation*Tests" test`

Result: 9 tests passed.

## P13C Runtime Additions

- Added `reserve_allocations`.
- Added reserve allocation status and purpose type.
- Added create/list/detail/update/release/cancel endpoints.
- Added validation for required name, positive amount, currency, purpose type, and same-workspace wallet/category/jar references.
- Added release behavior: `ACTIVE -> RELEASED`, repeated release returns released state, `CANCELLED` release blocked.
- Added cancel behavior: `ACTIVE -> CANCELLED`, repeated cancel returns cancelled state, `RELEASED` cancel blocked.
- Added `RESERVE_BALANCE_CHECK_UNAVAILABLE` warning because P13C does not invent an available-balance calculation.
- Added targeted integration tests proving workspace isolation and no transaction side effects.

Targeted tests:

`.\mvnw.cmd "-Dtest=*ReserveAllocation*Tests,*PlanningReserve*Tests" test`

Result: targeted reserve allocation tests passed.

## P13D Runtime Additions

- Added read-only `PlanningProjectionService`.
- Added projection DTOs for snapshot, breakdown evidence, and warnings.
- Reused `WalletBalanceService` as the available-ledger source.
- Added active reserve and planned obligation projection queries.
- Formula: `availableLedgerBalance - activeReserveAmount - upcomingRequiredOutflowAmount - overdueRequiredOutflowAmount`.
- Expected incoming remains zero and not spendable in P13D.
- No-wallet income remains excluded from available ledger.
- Warnings include negative/low spendable, projected shortfall, partial wallet data, expected-income unavailable, and mixed currency.
- P12D Financial Insight actually-spendable remains unchanged; later unification is deferred.

Targeted tests:

`.\mvnw.cmd "-Dtest=*PlanningProjection*Tests,*ProjectionService*Tests,*PlanningSpendable*Tests" test`

Result: targeted projection tests passed.

## P13E Runtime Additions

- Added `paid_at` and `paid_note` fields for planned obligations.
- Added unique `linked_transaction_id` index.
- Added `POST /api/workspaces/{workspaceId}/planning/obligations/{obligationId}/link-transaction`.
- Added `POST /api/workspaces/{workspaceId}/planning/obligations/{obligationId}/mark-paid`.
- Link-existing requires same-workspace posted non-deleted `EXPENSE` transaction with matching amount/currency and creates no transaction.
- Mark-paid requires same-workspace wallet/category and creates one posted expense through `TransactionService`.
- Repeated mark-paid/link returns existing paid state and does not duplicate transactions.
- Cancelled obligations cannot be paid.
- Due date alone never auto-pays.

Targeted tests:

`.\mvnw.cmd "-Dtest=*ObligationMarkPaid*Tests,*ObligationTransactionLink*Tests,*PlannedObligation*Tests" test`

Result: 15 tests passed.

## P13F Runtime Additions

- Added `GET /api/workspaces/{workspaceId}/planning/overview`.
- Added `GET /api/workspaces/{workspaceId}/planning/projection`.
- Added `GET /api/workspaces/{workspaceId}/planning/obligations/summary`.
- Added `GET /api/workspaces/{workspaceId}/planning/reserves/summary`.
- Added overview DTOs for projection, summaries, obligation/reserve lists, warnings, and action items.
- Added planning action items for overdue, due soon, projected shortfall, low spendable, active reserves, and partial planning data.
- Financial Insight action-item service remains untouched; overview-local action items are used in P13F.
- Overview/projection/summary endpoints are read-only and do not mutate transactions, obligations, or reserves.

Targeted tests:

`.\mvnw.cmd "-Dtest=*PlanningOverview*Tests,*PlanningController*Tests,*PlanningApi*Tests,*PlanningProjection*Tests" test`

Result: 20 tests passed.

## Non-Negotiables

- Planned obligation does not affect wallet balance until paid as actual transaction.
- Reserve allocation does not create or destroy money.
- Expected income is not actually spendable.
- Receivable debt is not spendable until collected.
- Payable debt is not normal expense.
- Wallet snapshots remain reconciliation evidence.
- Drafts and fake data do not affect planning.
- Workspace isolation is required for every endpoint.

## Known Limitations

- Frontend planning UI is not implemented yet.
- Recurring obligation generation is deferred.
- Notification/reminder system is deferred.
- AI planning summary is deferred.
- Undo/reopen paid obligation is deferred.
- Reserve usage workflow is deferred.
- Projection quality depends on user-entered obligations/reserves.
- Standalone payable debts are not included in the current planning formula.
- Reserve aggregates can overlap because cross-module deduplication is not inferred.
- Multi-currency projection is unsupported and returned with warnings.
- Financial Insight action-item cross-module integration is deferred.
